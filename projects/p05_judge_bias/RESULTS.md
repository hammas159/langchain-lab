# Results

Generated by `projects/p05_judge_bias/benchmark.py`. Do not edit.

3 judge prompts x 8 questions x 2 pair types x 2 orders x 2 repeats on `qwen2.5:3b-instruct`. 0.3 minutes.


## Tie pairs: two equally correct answers

There is no quality difference here, so the correct verdict is a tie and every preference recorded is bias. `position` is the share where the judge picked the same **slot** in both orders — a verdict decided by where the answer sat.

| judge prompt | flipped on reorder | position-decided | said tie | longer won |
|---|---|---|---|---|
| `plain` | 12% | 12% | 0% | 100% |
| `tie_allowed` | 50% | 0% | 88% | 100% |
| `rubric` | 12% | 0% | 12% | 100% |

## Quality pairs: one answer is wrong

`decisive` is the share of pairs the judge answered the same way in both orders; everything else is a coin flip dressed as a verdict. `correct when decisive` is accuracy over those.

| judge prompt | flipped on reorder | decisive | correct when decisive |
|---|---|---|---|
| `plain` | 44% | 56% | 100% |
| `tie_allowed` | 12% | 88% | n/a |
| `rubric` | 38% | 62% | 100% |

## Verdicts, as recorded


**`plain` / tie**

- `q1`: concise first -> B (verbose) | verbose first -> A (verbose)
- `q1`: concise first -> B (verbose) | verbose first -> A (verbose)
- `q2`: concise first -> B (verbose) | verbose first -> A (verbose)

**`plain` / quality**

- `q1`: concise first -> B (wrong) | wrong first -> B (concise)
- `q1`: concise first -> B (wrong) | wrong first -> B (concise)
- `q2`: concise first -> A (concise) | wrong first -> B (concise)

**`tie_allowed` / tie**

- `q1`: concise first -> B (verbose) | verbose first -> A (verbose)
- `q1`: concise first -> B (verbose) | verbose first -> A (verbose)
- `q2`: concise first -> B (verbose) | verbose first -> TIE (tie)

**`tie_allowed` / quality**

- `q1`: concise first -> TIE (tie) | wrong first -> TIE (tie)
- `q1`: concise first -> TIE (tie) | wrong first -> TIE (tie)
- `q2`: concise first -> TIE (tie) | wrong first -> B (concise)

**`rubric` / tie**

- `q1`: concise first -> B (verbose) | verbose first -> A (verbose)
- `q1`: concise first -> B (verbose) | verbose first -> A (verbose)
- `q2`: concise first -> B (verbose) | verbose first -> A (verbose)

**`rubric` / quality**

- `q1`: concise first -> A (concise) | wrong first -> B (concise)
- `q1`: concise first -> A (concise) | wrong first -> B (concise)
- `q2`: concise first -> A (concise) | wrong first -> B (concise)
